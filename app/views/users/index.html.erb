<div>
 <%= link_to new_user_path, :class => 'btn btn-primary' do %>
 <span class="glyphicon glyphicon-plus"></span>
   New User
  <% end %>
</div>
<div class="space"></div>
<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th>Latitude</th>
        <th>Longitude</th>
        <th>Name</th>
        <th>Address</th>
        <th>Email</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
      <td><%= user.latitude %></td>
      <td><%= user.longitude %></td>
      <td><%= user.name %></td>
      <td><%= user.email %></td>
      <td><%= user.address %></td>
      <td><%= user.postcode %></td>
      <!-- <td><%= user.phone %></td>
      <td><%= user.gender %></td>
      <td><%= user.license %></td>
      <td><%= user.car_make %></td>
      <td><%= user.car_colour %></td>
      <td><%= user.car_reg_no %></td>
      <td><%= user.smoking %></td>
      <td><%= user.pets %></td>
      <td><%= user.destination_id %></td> -->
    </tbody>
    <% end %>
  </table>
</div>
